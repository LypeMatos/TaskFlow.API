# TaskFlow.API
API REST para gerenciamento de tarefas (TaskFlow), desenvolvida com ASP.NET Core (.NET 8), utilizando Entity Framework Core e SQL Server. O projeto segue boas práticas como separação em camadas (Controllers, Services, DTOs) e aplicação de regras de negócio.
